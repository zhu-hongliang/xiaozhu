
spring.datasource.url=jdbc:mysql://localhost:3306/maintain?useUnicode=true&amp&characterEncoding=utf-8&allowMutilQueries=true&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=413135799

mybatis.config-location=classpath:mybatis-config.xml

#加载mapper配置文件，因为默认是加载mapper接口同级目录配置文件
mybatis.mapper-locations=classpath:mapper/*.xml

logging.level.root=debug

#spring.thymeleaf.prefix=classpath:/templates/html/
#
#spring.freemarker.prefix=classpath:/templates/fmt/
